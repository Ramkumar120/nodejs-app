apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-nodejs-deployment
  labels:
    app: my-nodejs
spec:
  replicas: 2
  selector:
    matchLabels:
      app: my-nodejs
  template:
    metadata:
      labels:
        app: my-nodejs
    spec:
      containers:
      - name: my-nodejs
        image: my-nodejs
        ports:
        - containerPort: 5006

